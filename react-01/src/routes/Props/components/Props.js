import React from 'react'
import Child from './Child'

export default class Props extends React.Component {
    render() {
        return (
            <div>
                Props
            </div>
        )
    }
}